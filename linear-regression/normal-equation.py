#!/usr/bin/python3
import numpy as np

if __name__ == "__main__":
    raw_data = np.loadtxt(open("Concrete_Data.csv", "rb"), delimiter=",", skiprows=1)
    X =
    theta = np.zeros(len(raw_data))


    v = 1